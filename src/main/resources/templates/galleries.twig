{% extends 'base.twig' %}

{% block content %}
<header>
    <div class="bg-image">
        <h3 class="ft-rw">Marketplace</h3>
        <h4 class="ft-rw">Search the galleries here</h4>
        <br>
        <form action="/galleries" method="get" id="search">
            <p>
                <input type="text" name="name" id="name_box">
                <input type="submit" name="info" value="Find">
                <br>
            </p>
        </form>
    </div>
</header>

<div class="wrapper-table">
    <section>
        <h2 class="table-title ft-rw">List of All Galleries</h2>
        <table>
            <tr class="table-head ft-rw">
                <th> Gallery Name </th>
                <th> Link </th>
            </tr>
            {% for row in galleries %}
            <tr>
                <td> {{ row.gallery_name }}</td>
                <td><a href="/gallery/{{ row.gallery_id }}"><span class="detail-button fc-white txt-sb"> View Gallery </span></a></td>
            </tr>
            {% endfor %}
        </table>
    </section>
</div>
{% endblock %}